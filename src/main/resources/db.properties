# Local TiDB Cloud credentials - NOT committed (.gitignore covers this file).
# If this file is missing, set DB_USERNAME / DB_PASSWORD environment variables instead.
db.username=*********
db.password=*********
